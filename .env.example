# AFO Configuration
# Copy to .env and adjust values

# MCP Server
MCP_HOST=127.0.0.1
MCP_PORT=8765

# Ollama (remote GPU machine)
OLLAMA_HOST=http://localhost:11434

# Database (Phase 2)
# POSTGRES_URL=postgresql://afo:afo@localhost:5432/afo
# CHROMA_HOST=localhost
# CHROMA_PORT=8000

# Security
# Set to 1 to require explicit approval for all deployments
REQUIRE_APPROVAL=1

# Rollback timeout in seconds (heartbeat failure triggers rollback)
ROLLBACK_TIMEOUT=30
